<%- include('partials/header') -%>

  <h1>Home</h1>
  <p><%= hStart %></p>

  <% posts.forEach(element => { %>
  <h1><%= element.title %></h1>
  <p><%= element.content.substr(1,100) %>... <a href= "/post/<%= element._id %>">Read More</a></p>
  <% }); %>

<%- include('partials/footer') -%>
